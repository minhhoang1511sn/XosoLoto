<Window x:Class="Xosoloto.VongLoaiSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cấu hình vòng loại" Height="550" Width="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Nhập số vòng loại và chọn màu vé tương ứng"
                   FontSize="14"
                   FontWeight="Bold"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"/>

        <!-- Import Excel Section -->
        <Border Grid.Row="1"
                BorderBrush="LightGray"
                BorderThickness="1"
                Padding="10"
                Margin="0,0,0,10"
                Background="#F5F5F5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="📁 Import Excel"
                        Padding="10,5"
                        Margin="0,0,10,0"
                        Click="ImportExcel_Click"/>

                <Button Grid.Column="1"
                        Content="📄 Tải Template"
                        Padding="10,5"
                        Margin="0,0,10,0"
                        Click="DownloadTemplate_Click"/>

                <TextBlock Grid.Column="2"
                           x:Name="ExcelStatusTextBlock"
                           Text="Chưa import file"
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           Foreground="Gray"/>
            </Grid>
        </Border>

        <!-- Column Header -->
        <Grid Grid.Row="2" Margin="0,0,0,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="65"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="155"/>
                <ColumnDefinition Width="35"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" Text="Số vòng" FontSize="11" Foreground="Gray" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="2" Text="Giá vé (VNĐ)" FontSize="11" Foreground="Gray" HorizontalAlignment="Center"/>
            <TextBlock Grid.Column="3" Text="Màu vé" FontSize="11" Foreground="Gray" HorizontalAlignment="Center"/>
        </Grid>

        <!-- Manual Input Section -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="VongLoaiStackPanel">
                <!-- Vòng mặc định -->
                <Grid Margin="0,3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="65"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="155"/>
                        <ColumnDefinition Width="35"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Vòng 1:"
                               VerticalAlignment="Center"
                               FontWeight="Bold"/>

                    <TextBox Grid.Column="1"
                             x:Name="Vong1TextBox"
                             FontSize="14"
                             Padding="5"
                             VerticalAlignment="Center"
                             Margin="0,0,8,0"/>

                    <TextBox Grid.Column="2"
                             x:Name="Vong1GiaVeTextBox"
                             FontSize="14"
                             Padding="5"
                             VerticalAlignment="Center"
                             Margin="0,0,8,0"/>

                    <ComboBox Grid.Column="3"
                              x:Name="Vong1ColorComboBox"
                              Height="32"
                              Margin="0,0,8,0"
                              SelectionChanged="ColorComboBox_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Rectangle Width="16" Height="16"
                                               Fill="{Binding Color}"
                                               Stroke="Gray"
                                               StrokeThickness="1"
                                               Margin="0,0,5,0"/>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <Button Grid.Column="4"
                            Content="X"
                            Width="30"
                            Height="30"
                            Click="RemoveVong_Click"
                            Tag="1"
                            IsEnabled="False"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Thêm vòng"
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Click="AddVong_Click"/>

            <Button Content="Xóa tất cả"
                    Padding="15,5"
                    Margin="0,0,10,0"
                    Click="ClearAll_Click"/>

            <Button Content="Xác nhận"
                    Padding="15,5"
                    IsDefault="True"
                    Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
